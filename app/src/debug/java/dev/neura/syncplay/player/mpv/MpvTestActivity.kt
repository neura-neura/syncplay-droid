package dev.neura.syncplay.player.mpv

import androidx.activity.ComponentActivity

/** Minimal debug-only host so surface tests do not create the production playback service. */
class MpvTestActivity : ComponentActivity()
